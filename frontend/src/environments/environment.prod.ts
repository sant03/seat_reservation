export const environment = {
  production: true,
  // Con el deploy único (backend de Render sirviendo el frontend) el mismo
  // origen es suficiente. Si desplegás el frontend por separado, poné la URL
  // del backend aquí, ej:
  // apiUrl: 'https://mi-bus-api.onrender.com'
  apiUrl: ''
};